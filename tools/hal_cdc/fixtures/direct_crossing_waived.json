{
  "version": 1,
  "description": "fixtures/direct_crossing.v with one scoped waiver and one stale waiver",
  "clocks": [
    {"name": "clk_a", "net": "clk_a", "period_ns": 10.0},
    {"name": "clk_b", "net": "clk_b", "period_ns": 7.0}
  ],
  "inputs": [
    {"net": "din", "clock": "clk_a"}
  ],
  "waivers": [
    {
      "id": "W-LOGIC-REG",
      "kind": "crossing",
      "source_domain": "clk_a",
      "destination_domain": "clk_b",
      "gates": ["logic_reg"],
      "rationale": "a_comb is a quasi-static configuration value that only changes while the clk_b block is held in reset; reviewed 2026-09 by the design owner"
    },
    {
      "id": "W-STALE",
      "kind": "crossing",
      "source_domain": "clk_b",
      "destination_domain": "clk_a",
      "rationale": "kept deliberately stale in this fixture so the 'waiver matched nothing' finding is exercised"
    }
  ]
}

# Quartus settings for walkthrough 16's core_e.
# Same device / family / flow as tools/hal_agilex/fixtures (see its README) and
# as every other walkthrough in the series.
set_global_assignment -name FAMILY "Agilex 3"
set_global_assignment -name DEVICE A3CW135BM16AE6S
set_global_assignment -name TOP_LEVEL_ENTITY core_e
set_global_assignment -name VERILOG_FILE core_e.v
set_global_assignment -name PROJECT_OUTPUT_DIRECTORY output_files

# Keep the load selection out of the flip-flop's `sload` and `sclr` ports and
# inside the LUT logic instead.  Both are real tennm_ff ports but they are
# outside the configuration hal_agilex validated (see tools/hal_agilex,
# "Coverage"), so an export using them would be reported `unsupported`.
set_global_assignment -name ALLOW_SYNCH_CTRL_USAGE OFF

# This design contains a 9 x 9 multiply, which Quartus puts in a tennm_mac (a
# DSP block) unless told not to.  A tennm_mac is not in hal_agilex's validated
# coverage, so the export would be reported `unsupported`.  The RTL says the
# same thing with a `multstyle = "logic"` attribute; this says it for the whole
# project.
set_global_assignment -name AUTO_DSP_RECOGNITION OFF
set_global_assignment -name DSP_BLOCK_BALANCING "LOGIC ELEMENTS"
